#include "GameCore.h"


      /*  while (window.pollEvent(event))
        {
            if (event.type == sf::Event::Closed)
                window.close();
        }*/

int main()
{
    GameCore gamecore;
    gamecore.start();


}